const {
    readFile
} = reqire('fs')

const { promisify } = require('util')

class Database {
    constructor(){
        this.NOME_ARQUIVO = 'herois.json'
    }
    obterArquivo(){

    }
    listar(){

    }
}